# Web Substrate Operations Gate

Before continuation, resolve or explicitly bound operational friction: unavailable source, rate limit, parser failure, provenance gap, conflicting evidence, stale data, authentication boundary, or policy/legal constraint.

Then continue with the next valid cycle.

`Always Do → Always Add → Solve Operations → Verify → Record → Continue.`
